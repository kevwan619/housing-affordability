[build-system]
requires = ["setuptools"]
build-backend = "setuptools.build_meta"

[project]
name = "housing-affordability"
version = "0.1.0"

[tool.setuptools]
package-dir = {"" = "src"}
packages = ["haf"]